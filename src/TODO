* sort the header file
* extract out lexer code
* replce GenerateCode by some sort of verbosity level: 
  - 0 : completely silent (identical as !GenerateCode), only used to check 
        tht a file CAN be parsed
  - 1 : normal parse
  - 2 : verbose, lloc for all tokens, incl comments
